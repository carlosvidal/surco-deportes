# ========================================
# CONFIGURACIÓN PARA DOCKER COMPOSE
# ========================================

# Base de datos PostgreSQL
DB_USER=surco
DB_PASSWORD=tu_password_postgres_muy_seguro_cambiar_esto
DB_NAME=surco_deportes

# Autenticación JWT
JWT_SECRET=tu_jwt_secret_muy_largo_minimo_32_caracteres_cambiar_esto

# Puertos (opcional, usa los defaults si no los especificas)
BACKEND_PORT=3000
FRONTEND_PORT=80

# URL de la API para el frontend
# En desarrollo local: http://localhost:3000/api
# En producción con nginx proxy: /api
VITE_API_URL=/api

# ========================================
# CONFIGURACIÓN PARA COOLIFY
# ========================================
# Coolify inyectará estas variables automáticamente
# Solo necesitas configurarlas en la interfaz de Coolify

# DATABASE_URL se construye automáticamente con:
# postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}

# NODE_ENV se establece automáticamente en "production"
